// Log in first
// Navigate to admin
// Test adding a new course (fill in different fields)
// Then cancel and check if all the fields get reset on the screen

describe("create a new course", () => {
  beforeEach(() => {
    cy.visit("/");
    cy.get()
  });

  // it('cancels adding a new course', () => {

  // })
});